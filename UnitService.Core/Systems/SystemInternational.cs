using UnitService.Core.Contracts;

namespace UnitService.Core.Systems
{
    /// <summary>
    /// This is the most widely used system of units, which is based on the metric system.
    /// The SI system has seven base units, which are the meter (length), kilogram (mass), 
    /// second (time), ampere (electric current), kelvin (temperature), 
    /// mole (amount of substance), and candela (luminous intensity).
    /// </summary>
    internal class SystemInternational : IUnitsSystem
    {
    }
}
